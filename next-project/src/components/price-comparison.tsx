"use client";

import React from "react";
// This component is no longer needed as we're using a pure pay-as-you-go model
// with no preset subscription plans

export function PriceComparison({ onApplyPreset }: { onApplyPreset: (preset: any) => void }) {
  // This component will render nothing as we've switched to a usage-based model
  return null;
}